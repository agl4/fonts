---
name: Iosevka

on:
  push:
    paths:
      - Makefile
      - make/iosevka.mk

jobs:
  install-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: make install-iosevka
      - run: make test-iosevka
