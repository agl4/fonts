---
name: Aporetic

on:
  push:
    paths:
      - Makefile
      - make/aporetic.mk

jobs:
  install-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make test-aporetic
      - run: make install-aporetic

  
