---
name: Aporetic

on:
  push:
    paths:
      - Makefile
      - make/aporetic.mk

jobs:
  install-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: make install-aporetic
      - run: make test-aporetic

  
