---
name: Input

on:
  push:
    paths:
      - Makefile
      - make/input.mk
  schedule:
    - cron: '42 23 13 */3 *'

jobs:
  install-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make install-input
      - run: make test-input
